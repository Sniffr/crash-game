# Odds harvester service deps.
#
# We drive Playwright directly (see oddsportal_listing.py) rather than using the
# `oddsharvester` package: its selectors no longer match OddsPortal's listing
# markup, so it returns zero rows for every date. Pin playwright to the base
# image's tag so the bundled browsers match the driver.
playwright==1.62.0
boto3>=1.34.0
