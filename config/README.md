# Drop a theme JSON in this folder named `active-theme.json`. The server reads it on boot and serves it as the default theme via /api/theme. Edits are picked up without restarting.
now i want you to serpate these types into two types of crash games ones that use sprites and one that use gifs for the gifs  ill add gifs for all the states and the multiplier
will be overlay on the gif ill have a gif for loading that is on the place bet you'll remind me of the states  so loading  started  a multiplier one maybe if 2.0x optional and
crashed  the gif has both the animation nd the  background s ot he background is unnecessary for it   so on the creator have that be the first toggle option on the type of
crash game 